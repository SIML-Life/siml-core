use crate::eden::grid::World;

pub struct MicroBiomeSystem {}

impl MicroBiomeSystem {
    pub fn new() -> Self {
        MicroBiomeSystem {}
    }

    pub fn update(&mut self, _world: &mut World) {
        // TODO: update microbes later
    }
}
